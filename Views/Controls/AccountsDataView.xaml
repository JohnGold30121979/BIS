<UserControl x:Class="BIS.ERP.Views.Controls.AccountsDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="SearchBox" Height="30" Padding="8,4" TextChanged="SearchBox_TextChanged" VerticalAlignment="Center" />
        </DockPanel>

        <DataGrid x:Name="AccountsGrid" Grid.Row="1"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="AccountsGrid_MouseDoubleClick"
                  SelectionChanged="AccountsGrid_SelectionChanged"
                  AlternatingRowBackground="#F5F5F5"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding [Код]}" Width="100"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding [Наименование]}" Width="300"/>
                <DataGridTextColumn Header="Тип счета" Binding="{Binding [Тип счета]}" Width="140"/>
                <DataGridTextColumn Header="Активен" Binding="{Binding [Активен]}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>