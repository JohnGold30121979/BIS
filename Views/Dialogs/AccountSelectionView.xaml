<UserControl x:Class="BIS.ERP.Views.AccountSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:BIS.ERP.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="720">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>      <!-- Таблица со своим скроллом: занимает оставшееся пространство -->
            <RowDefinition Height="Auto"/>   <!-- Кнопки — всегда видимы -->
        </Grid.RowDefinitions>

        <controls:AccountsDataView x:Name="AccountsView" Grid.Row="0" />

        <!-- Нижняя панель: слева — Добавить/Изменить, справа — Выбрать/Отмена -->
        <DockPanel Grid.Row="1" Margin="0,10,0,0" LastChildFill="False">

            <!-- Левая группа кнопок -->
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <Button x:Name="AddButton" Content="Добавить" Width="110" Height="35" Margin="0,0,8,0"
                        Click="AddButton_Click"
                        Background="#2980B9" Foreground="White"/>
                <Button x:Name="EditButton" Content="Изменить счёт" Width="130" Height="35"
                        Click="EditButton_Click"
                        IsEnabled="False"
                        Background="#8E44AD" Foreground="White"/>
            </StackPanel>

            <!-- Правая группа кнопок -->
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Выбрать" Width="100" Height="35" Margin="0,0,10,0"
                        Click="OkButton_Click"
                        Background="#27AE60" Foreground="White"/>
                <Button Content="Отмена" Width="100" Height="35"
                        Click="CancelButton_Click"
                        Background="#E74C3C" Foreground="White"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
