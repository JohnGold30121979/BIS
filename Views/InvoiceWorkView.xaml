<UserControl x:Class="BIS.ERP.Views.InvoiceWorkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="TitleText" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock x:Name="DescriptionText" FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="AddButton" Content="➕ Добавить" Width="100" Height="35" Margin="0,0,10,5"
                    Background="#27AE60" Foreground="White" Click="OnAddClick"/>
            <Button x:Name="EditButton" Content="✏️ Редактировать" Width="130" Height="35" Margin="0,0,10,5"
                    Background="#3498DB" Foreground="White" IsEnabled="False" Click="OnEditClick"/>
            <Button x:Name="ViewButton" Content="👁️ Просмотр документа" Width="150" Height="35" Margin="0,0,10,5"
                    Background="#95A5A6" Foreground="White" IsEnabled="False" Click="OnViewClick"/>
            <Button x:Name="DeleteButton" Content="🗑️ Удалить" Width="100" Height="35" Margin="0,0,10,5"
                    Background="#E74C3C" Foreground="White" IsEnabled="False" Click="OnDeleteClick"/>
            <Button x:Name="AllPostingsButton" Content="📒 Все проводки" Width="130" Height="35" Margin="0,0,10,5"
                    Background="#16A085" Foreground="White" IsEnabled="False" Click="OnAllPostingsClick"/>
            <Button x:Name="PrintButton" Content="🖨 Печатная форма" Width="145" Height="35" Margin="0,0,10,5"
                    Background="#34495E" Foreground="White" IsEnabled="False" Click="OnPrintClick"/>
            <Button x:Name="ExportEsfButton" Content="📤 Выгрузка ЭСФ" Width="135" Height="35" Margin="0,0,10,5"
                    Background="#8E44AD" Foreground="White" Click="OnExportEsfClick"/>
            <Button x:Name="ImportEsfButton" Content="📥 Загрузка ответа" Width="145" Height="35" Margin="0,0,10,5"
                    Background="#7F8C8D" Foreground="White" Click="OnImportEsfClick"/>
            <Button x:Name="RefreshButton" Content="🔄 Обновить" Width="100" Height="35" Margin="0,0,0,5"
                    Background="#95A5A6" Foreground="White" Click="OnRefreshClick"/>
        </WrapPanel>

        <DataGrid x:Name="InvoicesGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  SelectionChanged="OnInvoiceSelectionChanged" MouseDoubleClick="OnInvoiceDoubleClick"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Resources>
                <Style x:Key="ColumnFilterTextBoxStyle" TargetType="TextBox">
                    <Setter Property="MinWidth" Value="68"/>
                    <Setter Property="Height" Value="22"/>
                    <Setter Property="Margin" Value="0,4,0,0"/>
                    <Setter Property="Padding" Value="3,1"/>
                    <Setter Property="FontSize" Value="11"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DocDate, StringFormat=dd/MM/yyyy}" Width="105">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Дата"/>
                            <TextBox x:Name="DateFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>                
                <DataGridTextColumn Binding="{Binding DocNumber}" Width="120">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Документ"/>
                            <TextBox x:Name="DocumentFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding TotalAmount, StringFormat=N2}" Width="120">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Сумма"/>
                            <TextBox x:Name="AmountFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="EsfNumberColumn" Binding="{Binding EsfNumber}" Width="155">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Номер ЭСФ"/>
                            <TextBox x:Name="EsfNumberFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="TaxBlankColumn" Binding="{Binding TaxBlankNumber}" Width="170" Visibility="Collapsed">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Серия и № бланка"/>
                            <TextBox x:Name="TaxBlankFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding OrganizationName}" Width="*" MinWidth="220">
                    <DataGridTextColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Организация"/>
                            <TextBox x:Name="OrganizationFilterBox" Style="{StaticResource ColumnFilterTextBoxStyle}" TextChanged="OnColumnFilterChanged"/>
                        </StackPanel>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn x:Name="BasisColumn" Header="Основание" Binding="{Binding Basis}" Width="220"/>
                <DataGridTextColumn x:Name="EsfStatusColumn" Header="Статус документа" Binding="{Binding TaxStatusDisplay}" Width="125"/>
                <DataGridTextColumn x:Name="ModuleColumn" Header="Модуль" Binding="{Binding ModuleCode}" Width="115"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="Строки счета-фактуры" FontWeight="Bold" Margin="0,10,0,5"/>

        <DataGrid x:Name="LinesGrid" Grid.Row="4" AutoGenerateColumns="False" IsReadOnly="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  AlternatingRowBackground="#F8F8F8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn x:Name="LineUnitColumn" Header="ЕдИз" Binding="{Binding UnitName}" Width="80" Visibility="Collapsed"/>
                <DataGridTextColumn x:Name="LineQuantityColumn" Header="Кол-во" Binding="{Binding Quantity, StringFormat=N3}" Width="90" Visibility="Collapsed"/>
                <DataGridTextColumn x:Name="LineAccountColumn" Header="Счет" Binding="{Binding AccountName}" Width="220"/>
                <DataGridTextColumn Header="Сумма б/нал" Binding="{Binding AmountWithoutTax, StringFormat=N2}" Width="110"/>
                <DataGridTextColumn Header="НДС" Binding="{Binding VatAmount, StringFormat=N2}" Width="90"/>
                <DataGridTextColumn x:Name="LineSalesTaxColumn" Header="Налог с прод" Binding="{Binding SalesTaxAmount, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Итого" Binding="{Binding LineTotal, StringFormat=N2}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="5" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Готово" Foreground="Gray"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="Подсказка: двойной щелчок по счет-фактуре открывает просмотр без изменения данных."
                           Foreground="Gray"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
