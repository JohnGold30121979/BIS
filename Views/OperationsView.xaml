<UserControl x:Class="BIS.ERP.Views.OperationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📋 Операции" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Список всех операций" Foreground="#666" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="OperationsGrid" Grid.Row="1" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  RowHeaderWidth="0" AlternatingRowBackground="#F8F9FA"
                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№ п/п" Binding="{Binding LineNumber}" Width="60"/>
                <DataGridTextColumn Header="Код операции" Binding="{Binding OperationCode}" Width="100"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding OperationDescription}" Width="*"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>