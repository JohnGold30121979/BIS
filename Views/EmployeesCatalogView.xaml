<UserControl x:Class="BIS.ERP.Views.EmployeesCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="👥 Списочный состав" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Управление сотрудниками предприятия" FontSize="12" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- Панель инструментов -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <WrapPanel>
                <Button x:Name="AddButton" Content="➕ Добавить" Click="AddButton_Click" 
                        Background="#4CAF50" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button x:Name="EditButton" Content="✏️ Редактировать" Click="EditButton_Click" 
                        Background="#2196F3" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand" IsEnabled="False"/>
                <Button x:Name="DeleteButton" Content="🗑️ Удалить" Click="DeleteButton_Click" 
                        Background="#F44336" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand" IsEnabled="False"/>
                <Button x:Name="TerminateButton" Content="🚪 Уволить" Click="TerminateButton_Click" 
                        Background="#FF9800" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand" IsEnabled="False"/>
                <Button x:Name="ExportButton" Content="📎 Экспорт" Click="ExportButton_Click" 
                        Background="#9B59B6" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button x:Name="ImportButton" Content="📁 Импорт" Click="ImportButton_Click" 
                        Background="#00BCD4" Foreground="White" Padding="15,8" Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button x:Name="RefreshButton" Content="🔄 Обновить" Click="RefreshButton_Click" 
                        Background="#3498DB" Foreground="White" Padding="15,8"
                        Cursor="Hand"/>
            </WrapPanel>
        </Border>

        <!-- Поиск -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchBox" Grid.Column="0" 
                         TextChanged="SearchBox_TextChanged"
                         Height="35" Padding="10"/>

                <Button x:Name="SearchButton" Grid.Column="1" Content="🔍 Поиск" Click="SearchButton_Click" 
                        Width="80" Height="35" Margin="10,0,0,0"
                        Background="#3498DB" Foreground="White"/>
            </Grid>
        </Border>

        <!-- Таблица сотрудников -->
        <DataGrid x:Name="EmployeesGrid" Grid.Row="3"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  RowHeaderWidth="0"
                  AlternatingRowBackground="#F8F9FA"
                  Background="White"
                  BorderBrush="#E0E0E0"
                  BorderThickness="1"
                  RowHeight="35"
                  FontSize="13"
                  SelectionChanged="EmployeesGrid_SelectionChanged">

            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3498DB" Opacity="0.3"/>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Таб. номер" Binding="{Binding PersonnelNumber}" Width="100"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="150"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding Department}" Width="150"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Дата приема" Binding="{Binding HireDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Дата увольнения" Binding="{Binding TerminationDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Паспорт №/ID" Binding="{Binding PassportNumber}" Width="120"/>
                <DataGridTextColumn Header="Кем выдан" Binding="{Binding PassportIssuedBy}" Width="180"/>
                <DataGridTextColumn Header="Дата выдачи" Binding="{Binding PassportIssueDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="4" Margin="0,5,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Готов"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TotalInfo" Text=""/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
